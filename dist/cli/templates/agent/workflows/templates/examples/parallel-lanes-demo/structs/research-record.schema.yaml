name: research-record
description: Per-iteration research record (per_iteration — one file per lane loop pass).
version: 1
type: object
required:
  - topic_id
  - title
  - finding
  - iteration
properties:
  topic_id:
    type: integer
  title:
    type: string
  finding:
    type: string
  iteration:
    type: integer
