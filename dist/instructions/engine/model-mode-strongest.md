---
used-by: src/engine/workflow-engine.ts
---
MODEL (strongest): spawn the subagent for this step on the STRONGEST (most capable) model available on this host — the author marked this step as judgment-heavy; a failed attempt costs more than the model difference.
